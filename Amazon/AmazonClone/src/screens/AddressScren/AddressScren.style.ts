import {StyleSheet, Dimensions} from 'react-native';

export default StyleSheet.create({
  root: {
        padding:10,
  },
  row: {
        marginVertical:10,
  },
  errorLabel:{
        color:'red'
  }
  
});
